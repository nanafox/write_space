<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "WriteSpace - Your Space to Write and Share Ideas" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <!-- SEO Meta Tags -->
    <meta name="description" content="<%= content_for(:description) || 'WriteSpace gives every writer their own corner of the internet. Create engaging content, build your audience, and connect with a community that values great writing.' %>">

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website">
    <meta property="og:title" content="<%= content_for(:title) || 'WriteSpace - Your Space to Write and Share Ideas' %>">
    <meta property="og:description" content="<%= content_for(:description) || 'WriteSpace gives every writer their own corner of the internet. Create engaging content, build your audience, and connect with a community that values great writing.' %>">
    <meta property="og:url" content="<%= request.url %>">

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image">
    <meta property="twitter:title" content="<%= content_for(:title) || 'WriteSpace - Your Space to Write and Share Ideas' %>">
    <meta property="twitter:description" content="<%= content_for(:description) || 'WriteSpace gives every writer their own corner of the internet. Create engaging content, build your audience, and connect with a community that values great writing.' %>">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=Merriweather:wght@300;400;700;900&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="h-full bg-white font-sans antialiased">
    <%= yield %>
  </body>
</html>
